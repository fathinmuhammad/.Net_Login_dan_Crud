@model List<Test_FathinMuhammadFadhlullah.Entities.User>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

@section page_title{
    User Administration Data}

@section css{
    <!-- BEGIN VIEW LEVEL CSS -->
    <!-- END VIEW LEVEL CSS -->
}


@section Scripts{
    <!-- BEGIN VIEW LEVEL SCRIPTS -->
    <!-- END VIEW LEVEL SCRIPTS -->
}

<div class="page-title"></div>
<div class="row">
    <div class="col-md-12">
        @await Html.PartialAsync("_StatusMessage")
        <!-- start filter -->
        <div class="portlet light bordered">
            <div class="portlet-title">
                <div class="caption">
                    <i class="fa fa-filter font-green"></i>
                    <span class="caption-subject font-green bold uppercase"> Filter User</span>
                </div>
                <div class="tools"></div>
            </div>
            <div class="portlet-body form">
                <div class="form-horizontal">
                    <div class="form-body">
                        <div class="row">
                            <div class="col-md-6">
                                <div class="form-group">
                                    <label class="col-md-4 control-label">Username</label>
                                    <div class="col-md-8">
                                        <input type="text" class="form-control input-medium" id="username" name="username" value="@(ViewBag.username ?? String.Empty)">
                                    </div>
                                </div>
                                <div class="form-group">
                                    <label class="col-md-4 control-label">Fullname</label>
                                    <div class="col-md-8">
                                        <input type="text" class="form-control input-medium" id="fullname" name="fullname" value="@(ViewBag.fullname ?? String.Empty)">
                                    </div>
                                </div>
                            </div>
                            <div class="col-md-5">
                                <div class="form-group">
                                    <label class="col-md-2 control-label"></label>
                                    <div class="col-md-8">
                                        <button class="btn btn-primary" id="submitUsr" name="submit" value="findDoc"><i class="fa fa-search"></i> Search</button> &nbsp; &nbsp;
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        <!-- end of filter -->
        <div class="portlet light ">
            <div class="portlet-title">
                <div class="caption font-dark">
                    <i class="icon-settings font-dark"></i>
                    <span class="caption-subject bold uppercase"> User</span>
                </div>
                <div class="actions">
                    <div class="btn-group btn-group-devided">
                        <a href="@Url.Action("Add","User")" class="btn btn-circle btn-sm sbold red">
                            <i class="fa fa-plus"> </i>Add New
                        </a>
                    </div>
                </div>
            </div>
            <div class="portlet-body">
                <table class="table table-striped table-bordered table-hover" data-request-url="@Url.Action("IndexSearch", "User")" id="dtTables">
                    <thead>
                        <tr role="row">
                            <th>No.</th>
                            <th>Username</th>
                            <th>FullName</th>
                            <th>Action</th>
                        </tr>
                    </thead>
                    <tbody>
                        @*@{
                            for (int i = 0; i < Model.Count; i++)
                            {
                                <tr>
                                    <td>@(i + 1)</td>
                                    <td>@Model[i].username</td>
                                    <td>@Model[i].fullname</td>
                                    <td>
                                        <a href="@Url.Action("Edit","User", new { code = Model[i].user_id })" class="icon huge" title="Details User"><i class="fa fa-pencil"></i></a>&nbsp;
                                        <a href="@Url.Action("Delete","User", new { code = Model[i].user_id })" class="icon huge" title="Delete User"><i class="fa fa-trash-o" onclick="return confirm('are you sure ?')"></i></a>&nbsp;
                                    </td>
                                </tr>
                            }
                        }*@
                    </tbody>
                </table>
            </div>
        </div>
        <!-- END EXAMPLE TABLE PORTLET-->
    </div>
</div>